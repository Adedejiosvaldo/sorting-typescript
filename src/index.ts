// Bubble Sort

//Bubble Scaffolfing

class Sorter {
  //   collection: number[];
  constructor(public collection: number[]) {}

  //SOrt function
  sort(): void {}
}

const sorter = new Sorter([1, 2, 3]);
console.log(sorter);
